<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.digitalsoftware.accounting.mapper.DAO.UserMapper">
    <sql id="Base_Column_List">
        `id`,
        `avatar`,
        `phone`,
        `password`,
        `nickname`,
        `details`
    </sql>
    <resultMap id="BaseResultMap" type="com.digitalsoftware.accounting.domain.generated.User">
        <result column="id" property="id"/>
        <result column="avatar" property="avatar"/>
        <result column="phone" property="phone"/>
        <result column="password" property="password"/>
        <result column="nickname" property="nickname"/>
        <result column="details" property="details"/>
    </resultMap>

    <resultMap id="UserRole" type="com.digitalsoftware.accounting.domain.models.UserRole">
        <result column="id" property="id"/>
        <association property="user" resultMap="BaseResultMap"/>
        <collection property="roleTypes" javaType="list" ofType="com.digitalsoftware.accounting.emun.domain.RoleType">
            <result column="role" property="roleTypes"/>
        </collection>
    </resultMap>

    <!--auto generated by codehelper on 2018-10-09-->
    <select id="findByPhone" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from `user`
        where 1=1
        <if test="phone!=null">
            and `phone`=#{phone}
        </if>
    </select>

    <!--auto generated by codehelper on 2018-10-09-->
    <select id="findOneByPhone" resultMap="UserRole">
        select u.*, r2.role
        from user u
                 join user_has_role r on u.id = r.user_id
                 join role r2 on r.role_id = r2.id
        where u.phone = #{phone}
    </select>
    <select id="existsByPhone" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM user WHERE phone = #{phone})
    </select>


</mapper>
